<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Student Voting</title>
    <link href="/css/font.css" rel="stylesheet">
    <link href="/css/style.min.css" rel="stylesheet" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

</head>

<body class="bg-gradient-to-br from-gray-100 to-gray-50 font-sans flex flex-col min-h-screen">
    <div id="loader" class="fixed inset-0 bg-white z-50 flex items-center justify-center">
        <img src="/images/loader-green.gif" alt="Loading..." class="w-20 h-20">
    </div>

    <header class="bg-gradient-to-r from-red-700 to-red-900 text-white p-4 shadow-lg relative z-10">
        <div class="container mx-auto flex justify-between items-center">
            <div class="text-2xl font-extrabold tracking-tight flex items-center">
                <span>Student Login</span>
            </div>
            <a href="/"
                class="bg-white text-red-700 px-5 py-2 rounded-full shadow-md hover:bg-gray-100 transition transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-white focus:ring-opacity-50">
                Home
            </a>
        </div>
    </header>

    <div id="main-content" class="flex-grow flex items-center justify-center px-4 py-10 hidden">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-4xl flex overflow-hidden border border-gray-200">
            <div class="hidden md:flex w-1/2 justify-center items-center px-10 bg-red-50">
                <div class="text-center">
                    <img src="<%= schoolLogo %>" alt="School Logo"
                        class="w-24 h-24 md:w-28 md:h-28 rounded-full shadow-md border-4 border-red-800 mx-auto mb-5" />
                    <h1 class="text-3xl md:text-4xl font-extrabold text-red-900">Welcome Back</h1>
                    <p class="text-base md:text-lg text-gray-700 mt-3 leading-relaxed hidden lg:block">
                        Log in to securely vote in the <%= schoolName %> election.<br>
                        <span class="font-medium">Your vote matters — make it count.</span>
                    </p>
                </div>
            </div>

            <div class="w-full md:w-1/2 p-8 lg:p-10 flex flex-col justify-center">
                <h2 class="text-2xl md:text-3xl font-bold text-red-900 mb-6 text-center">Student Login</h2>

                <% if (typeof messages !== 'undefined' && messages.length > 0) { %>
                    <% messages.forEach(function(msgObj) { %>
                        <% var message = typeof msgObj === 'string' ? msgObj : msgObj.msg; %>
                        <% var type = typeof msgObj === 'string' ? 'error' : (msgObj.type || 'error'); %>
                        <div class="text-red-600 text-sm mb-4 text-center font-medium">
                            <%= message %>
                        </div>
                    <% }); %>
                <% } %>

                <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                    <div class="text-red-600 text-sm mb-4 text-center font-medium">
                        <%= errorMessage %>
                    </div>
                <% } %>

                <form action="/login" method="POST" class="space-y-6">
                    <div>
                        <label for="studentId"
                            class="block text-sm font-semibold text-gray-700 mb-2">Student ID</label>
                        <input type="text" id="studentId" name="studentId" placeholder="Enter your student ID" required
                            class="w-full px-4 py-3 rounded-lg bg-gray-50 text-gray-800 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent shadow-sm transition duration-200" />
                    </div>
                    <div>
                        <label for="password"
                            class="block text-sm font-semibold text-gray-700 mb-2">Password</label>
                        <div class="relative">
                            <input type="password" id="password" name="password" placeholder="Enter your password" required
                                class="w-full px-4 py-3 rounded-lg bg-gray-50 text-gray-800 border border-gray-300 pr-10 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent shadow-sm transition duration-200" />
                            <button type="button" id="togglePassword"
                                class="absolute inset-y-0 right-0 pr-3 flex items-center text-gray-600 hover:text-gray-900 focus:outline-none">
                                <i id="eyeIcon" class="fas fa-eye"></i>
                            </button>
                        </div>
                    </div>
                    <button type="submit"
                        class="w-full bg-red-700 hover:bg-red-800 text-white font-semibold text-lg py-3 px-4 rounded-lg shadow-md focus:outline-none focus:ring-2 focus:ring-red-400 transition duration-300 ease-in-out transform hover:scale-105">
                        Sign In
                    </button>
                </form>

                <div class="flex flex-col sm:flex-row justify-between items-center mt-6 text-sm">
                    <a href="/" class="text-red-700 font-medium hover:underline mb-2 sm:mb-0">← Return Home</a>
                    <a href="#" id="forgot-password-link" class="text-red-700 font-medium hover:underline">Report Issue</a>
                </div>

                <!-- Issue Form -->
                <div id="issueFormContainer" class="mt-6 p-4 bg-gray-50 rounded-lg border border-gray-200 hidden">
                    <h3 class="text-lg font-medium text-gray-800 mb-3">Report an Issue</h3>
                    <form action="/submit-issue" method="POST" class="space-y-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Your Name</label>
                            <input type="text" name="name" placeholder="Enter your name" required
                                class="w-full px-4 py-2 rounded-lg bg-white text-gray-800 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent shadow-sm">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Your Class</label>
                            <input type="text" name="className" placeholder="Enter your class" required
                                class="w-full px-4 py-2 rounded-lg bg-white text-gray-800 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent shadow-sm">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Issue/Problem</label>
                            <textarea name="problem" placeholder="Describe your issue or problem" required
                                class="w-full px-4 py-2 rounded-lg bg-white text-gray-800 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent shadow-sm h-20"></textarea>
                        </div>
                        <div class="flex justify-end space-x-3">
                            <button type="button" id="cancel-issue-btn"
                                class="px-4 py-2 bg-gray-500 hover:bg-gray-600 text-white font-medium rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-gray-400">
                                Cancel
                            </button>
                            <button type="submit"
                                class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white font-medium rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-red-400">
                                Submit Issue
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-red-900 text-white text-center py-4 flex-shrink-0">
        <p class="text-sm font-medium">&copy; <%= new Date().getFullYear() %> Voting App. All rights reserved.</p>
    </footer>

    <script src="/js/preloader.js"></script>
    <script src="/js/login.js"></script>
</body>

</html>
