<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Vote Slip</title>
    <link href="/css/font.css" rel="stylesheet">
   <link href="/css/style.min.css" rel="stylesheet" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body class="bg-gradient-to-br from-gray-100 to-gray-50 font-sans flex flex-col min-h-screen">
    <header class="bg-gradient-to-r from-red-700 to-red-900 text-white p-4 shadow-lg relative z-10">
        <div class="container mx-auto flex justify-between items-center">
            <div class="text-2xl font-extrabold tracking-tight flex items-center">

                <span>Vote Confirmed!</span>
            </div>
            <a href="/login"
                class="bg-white text-red-700 px-5 py-2 rounded-full shadow-md hover:bg-gray-100 transition transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-white focus:ring-opacity-50">
                Logout

            </a>
        </div>
    </header>

    <main class="flex-grow flex items-center justify-center p-4">
        <div
            class="bg-white rounded-2xl shadow-xl overflow-hidden w-full max-w-2xl border border-gray-200 animate-fadeIn">
            <!-- Header Section -->
            <div class="bg-gradient-to-r from-red-900 to-red-700 p-6 text-center">
                <div class="flex justify-center mb-3">
                    <div class="bg-white/20 p-3 rounded-full">
                    </div>
                </div>
                <h1 class="text-2xl md:text-3xl font-bold text-white">Vote Successfully Recorded!</h1>
                <p class="text-red-100 mt-2">Thank you for participating.</p>
            </div>

            <!-- Main Content -->
            <div class="p-6 md:p-8">
                <p class="text-gray-700 text-center mb-6 font-medium">
                    Your vote matters. Here is a summary of your selections:
                </p>

                <div class="border rounded-xl p-5 md:p-6 bg-red-50/30 border-red-200 shadow-sm">
                    <% if (votedCandidates && votedCandidates.length> 0) { %>
                        <ul class="space-y-4">
                            <% votedCandidates.forEach((vote, index)=> { %>
                                <li
                                    class="flex flex-col sm:flex-row sm:justify-between sm:items-center p-3 rounded-lg hover:bg-red-50 transition-colors duration-200">
                                    <div class="flex items-center mb-2 sm:mb-0">
                                        <span class="bg-red-100 text-red-800 text-xs font-bold px-2 py-1 rounded mr-2">
                                            <%= index + 1 %>
                                        </span>
                                        <% if (vote.candidateId && vote.candidateId.image) { %>
                                            <img src="<%= vote.candidateId.image %>" alt="<%= vote.name %>"
                                                class="w-10 h-10 rounded-full object-cover mr-3">
                                            <% } %>
                                                <span class="font-semibold text-gray-800">
                                                    <%= vote.position %>
                                                </span>
                                    </div>
                                    <span
                                        class="text-gray-900 font-medium bg-white px-3 py-1 rounded-md border border-red-100 shadow-xs">
                                        <%= vote.name %>
                                    </span>
                                </li>
                                <% }) %>
                        </ul>
                        <% } else { %>
                            <div class="text-center py-6">
                                <p class="text-gray-500">
                                    No vote summary available. An error might have occurred.
                                </p>
                            </div>
                            <% } %>
                </div>

                <!-- Action Buttons -->
                <div class="mt-8 flex flex-col sm:flex-row justify-center gap-4">
                    <a href="/"
                        class="px-5 py-2.5 text-center text-sm font-medium text-white bg-red-700 rounded-lg hover:bg-red-800 focus:ring-2 focus:ring-red-400 transition duration-300 ease-in-out shadow-sm">

                        Dashboard
                    </a>
                    <a href="/login"
                        class="px-5 py-2.5 text-center text-sm font-medium text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 focus:ring-2 focus:ring-gray-300 transition duration-300 ease-in-out shadow-sm">

                        Logout
                    </a>
                </div>
            </div>

            <!-- Footer Note -->
            <div class="bg-gray-50 px-6 py-4 text-center border-t border-gray-200">
                <p class="text-xs text-gray-500">

                    Your vote is secure and anonymous.
                </p>
            </div>
        </div>
    </main>
