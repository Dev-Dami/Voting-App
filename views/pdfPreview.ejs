<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF Preview - Election Results</title>
    <link href="/css/tailwind.min.css" rel="stylesheet" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
        }
        .pdf-container {
            background-color: white;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
            margin: 20px auto;
            max-width: 900px;
        }
        .header {
            background: linear-gradient(135deg, #b91c1c, #dc2626);
            color: white;
            padding: 20px;
        }
        .controls {
            background-color: #f8fafc;
            padding: 15px;
            border-bottom: 1px solid #e5e7eb;
        }
        .pdf-viewer {
            height: 70vh;
            width: 100%;
        }
        .action-buttons {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }
        .btn {
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 600;
            cursor: pointer;
            border: none;
        }
        .btn-download {
            background-color: #1d4ed8;
            color: white;
        }
        .btn-download:hover {
            background-color: #1e40af;
        }
        .btn-back {
            background-color: #6b7280;
            color: white;
        }
        .btn-back:hover {
            background-color: #4b5563;
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="pdf-container">
        <div class="header">
            <h1 class="text-2xl font-bold">Election Results PDF Preview</h1>
            <p class="text-red-100">Preview and download your election results report</p>
        </div>
        
        <div class="controls">
            <div class="flex flex-col md:flex-row md:items-center md:justify-between">
                <div>
                    <h2 class="text-lg font-semibold text-gray-800">Election Results Report</h2>
                    <p class="text-sm text-gray-600">Generated on <%= new Date().toLocaleString() %></p>
                </div>
                
                <div class="action-buttons mt-3 md:mt-0">
                    <a href="/admin/export-pdf" class="btn btn-download">
                        Download PDF
                    </a>
                    <a href="/admin/dashboard" class="btn btn-back">
                        Back to Dashboard
                    </a>
                </div>
            </div>
        </div>
        
        <div class="pdf-viewer">
            <iframe 
                src="<%= pdfData %>" 
                width="100%" 
                height="100%"
                style="border: none;"
                title="Election Results PDF Preview"
            ></iframe>
        </div>
    </div>

    <script>
        // Optional: Add functionality to refresh the PDF preview if needed
        document.addEventListener('DOMContentLoaded', function() {
            // You can add additional JavaScript here if needed
        });
    </script>
</body>
</html>